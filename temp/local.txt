

2.>>>> upload file functionality:

npm instsll --save react-dropzone ------

! rectify the bucketFileId attribut in appWrite ------

! add the index.d.ts file ----

! install deps: soneer(replacement for toast), dropzone --------

! import and add toast to app layout ----

! pass props properly -----

!! nextjs payload limit by default is only 4mb -> must add experimental object to the nextjs.config - watch from tutorial 

! might need to turn eslint rule off for typescript interface auto loading from index.ts file

* test edge cases for upload files function